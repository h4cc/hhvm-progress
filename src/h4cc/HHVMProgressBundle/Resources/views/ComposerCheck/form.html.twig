
{% extends "base.html.twig" %}

{% block page_title %}Check your composer depenedencies - {{ parent() }}{% endblock %}

{% block body %}
    <h1>Upload composer.lock</h1>

   <div>

        <form action="{{ path('h4cc_hhvm_progress_composer_form') }}" method="post" enctype="multipart/form-data">

            <p>Upload your <b>composer.lock</b> file, to see which of your dependencies are tested against HHVM.</p>
            <input type="file" name="composer_lock" required>
            <br/>

            <p><b>Optional</b>: When you also upload your <b>composer.json</b> file, a dependency graph can be generated.</p>
            <input type="file" name="composer">
            <br/>


            <button type="submit" class="btn btn-primary">Upload</button>
        </form>

    </div>

    <p>
        <em>We do <b>not</b> store the content of your composer.lock file or process it otherwise than stated.</em>
    </p>

{% endblock %}